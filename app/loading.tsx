import FaceoffPreloader from "@/app/components/FaceoffPreloader";

export default function Loading() {
  return <FaceoffPreloader message="CONNECTING TO STADIUM & FETCHING DATA..." variant="fullscreen" />;
}
